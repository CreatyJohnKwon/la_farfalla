# 1. Base Image 설정
FROM node:18-alpine

# 2. 작업 디렉토리 생성
WORKDIR /app

# 3. 의존성 설치를 위해 package.json과 lock 파일 먼저 복사
COPY package*.json ./

# 4. 모든 의존성 설치 (개발용 의존성 포함)
# 'next.config.ts'를 빌드하기 위해 typescript가 필요하므로 --omit=dev를 제거합니다.
RUN npm ci

# 5. 전체 소스 코드 복사
COPY . .

# 6. Next.js 애플리케이션 빌드
RUN npm run build

# 7. 포트 노출
EXPOSE 3000

# 8. 컨테이너 시작 명령어
CMD ["npm", "start"]
