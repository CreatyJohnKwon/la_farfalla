# 1. Base Image 설정
FROM node:18-alpine

# 2. 작업 디렉토리 생성
WORKDIR /app

# 3. 의존성 설치를 위해 package.json과 lock 파일 먼저 복사
# (캐시를 활용하여 빌드 속도 향상)
COPY package*.json ./

# 4. 프로덕션 의존성 설치
RUN npm ci --omit=dev

# 5. 전체 소스 코드 복사 (tsconfig.json 등 설정 파일 포함)
COPY . .

# 6. Next.js 애플리케이션 빌드 (시크릿 없이!)
RUN npm run build

# 7. 포트 노출
EXPOSE 3000

# 8. 컨테이너 시작 명령어
CMD ["npm", "start"]

